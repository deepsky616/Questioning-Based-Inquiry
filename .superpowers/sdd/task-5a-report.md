# 작업 5가 보고서

## 구현

- 한국어와 영어 이름 및 별칭과 열네 속성값을 가진 내장 물건 자료를 추가했다.
- 질문에서 지원 속성이 정확히 하나일 때만 `yes` 또는 `no`를 만들고, 한 번 부정은 뒤집으며 겹친 부정은 `unknown`으로 처리한다.
- 추측은 `NFKC`, 앞뒤 공백 제거, 이어진 공백 축약과 영어 소문자 변환 뒤 이름 또는 별칭 전체와만 맞춘다.
- `mystery-box`를 중앙 정적 판정기 등록부에 넣었다. 미스터리 판정기의 중앙 판정기 가져오기는 형 전용이라 실행 중 순환은 없다.
- 일반 시작은 비밀 없는 버전 2 `setup` 상태를 만들고, 방장 `mystery-start`가 서버 물건과 첫 라운드 및 차례를 정한다.
- 질문은 서버 판정 기록과 질문자 점수만 더하며 차례, 활동 수와 라운드를 넘기지 않는다.
- 틀린 추측만 활동과 라운드 및 차례를 한 번 넘긴다. 정답 또는 스무 번째 오답은 같은 저장 후보에서 기록, 공개 정답과 방 종료를 함께 만든다.
- 저장 상태와 공개 상태 판독을 분리했다. 비공개 물건 식별값은 저장된 `gameState.private.itemId`에만 있고 공개 판독은 `private`이 있으면 거절한다.
- 같은 명령 재생, 재접속 상태, 진행 전과 진행 중 이탈, 참가자 부족 종료와 실제 요청 경로의 성공 및 조회 및 재생 및 충돌 및 나가기 및 다시 시작 응답을 시험했다.

## 시험 우선 확인

- 순수 규칙 첫 실행은 새 모듈을 찾지 못해 시험 파일 1개가 실패했다.
- 순수 규칙 첫 구현 뒤 모호한 한국어 부정 1개가 `unknown` 대신 `no`로 나와 실패했고 `아닌` 꼴을 별도 부정으로 세어 고쳤다.
- 첫 엔진 실행은 등록 판정기가 없어 준비와 방장 시작 2개가 `changed` 대신 `corrupt`로 실패했다.
- 질문과 차례 시험은 지원 명령 부재로 3개가 실패했다.
- 틀린 추측, 정답, 스무 번째 오답은 각 흐름을 넣기 전에 각각 1개가 `invalid`로 실패했다.
- 참가자 부족 종료는 공개 정답이 없어 1개가 실패했다.
- 기존 등록부 시험은 미스터리 판정기 추가 뒤 기대값이 달라 1개가 실패했다.
- 엄격 저장 판독 시험은 비공개 물건 누락, 라운드 불일치, 차례 범위, 점수 관계, 조기 정답 공개와 알 수 없는 물건 6개를 처음에 받아들여 실패했다.
- 공개 상태 전용 판독 시험은 함수가 없어 1개가 실패했다.
- 자기 검토에서 물건 준비 전 참가자 부족 종료 상태를 다시 읽지 못하는 1개 실패를 확인해 고쳤다.
- 첫 형 검사는 공개 정답의 좁힌 형이 배열 찾기 콜백에서 유지되지 않아 2개 오류가 났고, 검증된 지역 상수로 고친 뒤 통과했다.
- 실제 요청 경로의 공개 변환은 기존 공용 변환기가 이미 원본 불변과 비공개 제거를 만족해 처음부터 통과했으며 제품 요청 경로는 바꾸지 않았다.

## 최종 확인

```text
npm test -- src/__tests__/mystery-box-rules.test.ts src/__tests__/question-game-room-engine-mystery.test.ts src/__tests__/question-game-room-engine.test.ts src/__tests__/question-game-room-response.test.ts src/__tests__/game-room-route.test.ts
PASS 5 files, 204 tests, FAIL 0

npx tsc --noEmit
통과

npx eslint src/lib/mystery-box-rules.ts src/lib/question-game-room-engines/mystery.ts src/lib/question-game-room-engine.ts src/__tests__/mystery-box-rules.test.ts src/__tests__/question-game-room-engine-mystery.test.ts src/__tests__/question-game-room-engine.test.ts src/__tests__/game-room-route.test.ts
통과

git diff --check
통과
```

## 커밋

- `e2ba703` `fix: 미스터리 박스 서버 판정 추가`

## 남은 우려

- 이번 묶음은 순수 규칙, 서버 상태와 비공개 응답만 맡았다. 친구 방 화면과 지역 화면 종료 연결은 작업 5나 범위다.
- 질문 판정은 의도대로 고정 표현 사전을 사용한다. 지원하지 않는 표현은 `unknown`이며 자유 입력을 외부 추론에 보내지 않는다.
- 내장 물건 목록과 지역 화면의 기존 목록은 아직 서로 다른 파일에 있다. 화면 파일을 바꾸지 말라는 범위에 따라 이번 작업에서는 합치지 않았다.
- 전체 저장소 시험과 제품 빌드는 이번 작은 묶음의 완료 기준이 아니어서 실행하지 않았다. 지정 시험과 형 검사 및 수정 파일 코드 검사만 실행했다.
- 데이터베이스와 자료 이전 파일은 바꾸지 않았다.

## 독립 검토 보정

### 구현

- 한국어 속성 표현에 낱말 경계를 적용해 `사다리`, `꽃게`, `큰일`, `작은아버지`, `비행기` 같은 합성 낱말을 속성으로 잘못 읽지 않게 했다.
- 부정 표현의 위치를 기록해 속성 표현에 직접 붙은 부정만 값을 뒤집는다. 다른 절의 부정과 같은 속성에 긍정 및 부정이 함께 나온 질문은 `unknown`으로 처리한다.
- 질문과 추측 기록에 언어를 저장하고, 상태를 읽을 때 비밀 물건과 기록 언어로 답 및 정오를 다시 계산한다.
- 상태, 비공개 값, 공개 정답, 질문 기록과 추측 기록의 허용 키를 정확히 고정했다. 점수 키는 차례 참가자와 같아야 하며 명령 경계에서는 실제 방 참가자와도 맞아야 한다.
- 정답 추측과 스무 번째 오답은 전체 기록의 마지막 항목이어야 한다. 종료 뒤 붙인 질문이나 조작한 판정 기록은 거절한다.
- 미스터리 공개 상태는 허용 필드만 새 객체로 투영한다. 진행 중 정답, 최상위 및 중첩 비공개 복사본과 알 수 없는 키를 버리며 원본은 바꾸지 않는다.
- 방 식별값이나 상태 식별값 중 하나라도 미스터리 박스를 가리키면 미스터리 공개 투영을 사용해 식별값 불일치 상태도 비공개 복사본을 내보내지 않는다.
- 이탈 중에는 공통 처리로 차례만 먼저 줄어든 정상 불일치만 복원한다. 원래부터 빠진 참가자나 중복 차례는 손상으로 거절하고, 완료 및 인원 부족 방의 마지막 참가자는 저장하지 않고 실제 삭제 흐름까지 도달한다.

### 시험 우선 확인

- 한국어 경계와 절 단위 부정 시험 첫 실행은 33개 중 6개가 실패했다. 규칙 보정 뒤 33개가 모두 통과했다.
- 상태 검증 시험 첫 실행은 27개 중 10개가 실패했다. 기록 언어, 추가 키, 저장 판정 의미, 종료 기록 순서와 점수 키 관계를 보강한 뒤 이탈 관련 기존 2개만 남았다.
- 공개 응답 및 실제 경로 첫 실행은 115개 중 중첩 비공개 복사본 2개와 이탈 1개가 실패했다. 허용 필드 투영 뒤 중첩 시험 2개가 통과했다.
- 마지막 참가자 실제 삭제 시험은 올바른 요청 본문으로 다시 실행했을 때 완료 및 인원 부족 두 경우 모두 500 응답으로 실패했다. 이탈 복원 보정 뒤 관련 6개가 모두 통과했다.
- 추가 독립 검토에서 합성 낱말 3개, 같은 속성의 엇갈린 부정 2개와 식별값 불일치 공개 누출 1개를 실패로 재현했다. 보정 뒤 규칙 및 공개 응답 43개가 모두 통과했다.
- 빠진 참가자와 중복 차례를 이탈 중 정상화하는 손상 상태 2개를 실패로 재현했다. 복원 조건을 좁힌 뒤 미스터리 판정기 29개가 모두 통과했다.
- 최신 전체 시험 첫 실행은 상태가 생략된 방 생성 시험 6개가 공개 변환의 선택 접근 누락으로 실패했다. 호환 보정 뒤 전체 시험이 통과했다.

### 최종 확인

```text
npm test -- src/__tests__/mystery-box-rules.test.ts src/__tests__/question-game-room-engine-mystery.test.ts src/__tests__/question-game-room-engine.test.ts src/__tests__/question-game-room-response.test.ts src/__tests__/game-room-route.test.ts
PASS 5 files, 227 tests, FAIL 0

npm test
PASS 210 files, 1743 tests, FAIL 0

npx tsc --noEmit
통과

npx eslint src/lib/mystery-box-rules.ts src/lib/question-game-room-engines/mystery.ts src/lib/question-game-room-response.ts src/__tests__/mystery-box-rules.test.ts src/__tests__/question-game-room-engine-mystery.test.ts src/__tests__/question-game-room-response.test.ts src/__tests__/game-room-route.test.ts
통과

git diff --check
통과
```

### 커밋

- `d7389f6` `fix: 미스터리 박스 검증 경계 보강`

### 남은 우려

- 순수 상태 판독기는 방 참가자 목록을 입력받지 않는다. 저장 상태 내부의 점수와 차례 관계는 직접 검증하고, 실제 방 참가자 관계는 명령 및 이탈 경계에서 검증한다.
- 자유 입력 판정은 계속 고정 표현 사전 기반이다. 지원하지 않거나 뜻이 섞인 표현은 보수적으로 `unknown`을 반환한다.
- 친구 방 화면과 지역 화면 연결은 작업 5나 범위이며 이번 서버 보정에서는 화면 파일을 바꾸지 않았다.
- 데이터베이스와 자료 이전 파일은 바꾸지 않았다.

## 두 번째 재검토 보정

### 구현

- 질문과 추측을 모두 한 활동 및 한 라운드로 통일했다. `unknown` 질문도 기록과 점수를 반영한 뒤 차례를 넘기고 새 `roundId`와 다음 `round`를 만든다.
- 진행 및 인원 부족 상태의 라운드는 전체 `history` 길이에 하나를 더한 값으로 검증한다. 정답 추측 종료는 전체 기록 길이와 같고, 정답이 없는 종료는 질문 및 추측을 합친 기록이 스무 개여야 한다.
- 질문과 오답이 공통 활동 전이를 사용한다. 스무 번째 활동이 정답 추측이 아니면 질문 또는 오답 구분 없이 같은 저장 후보에서 정답 공개와 방 종료를 함께 만든다.
- 한국어 속성 패턴 전체를 왼쪽 및 오른쪽 낱말 경계를 쓰는 명사 또는 활용형 헬퍼로 통일했다. `실내화`와 `젖소` 같은 합성 낱말은 속성으로 읽지 않으며 자연스러운 조사와 활용형은 유지한다.
- 영어 부정과 속성 사이에 `a`, `an`, `the` 관사를 허용했다. 다른 절에 있는 부정은 계속 `unknown`으로 처리한다.
- 공통 이탈 처리에서 줄이기 전 차례에 이탈자가 정확히 한 번 있었는지를 문맥으로 전달한다. 미스터리 복원은 이 값과 정확한 점수 참가자 집합을 모두 만족할 때만 진행한다.

### 시험 우선 확인

- 전체 활동 라운드 시험 첫 실행은 미스터리 판정기 34개 중 6개가 실패했다. 질문 차례 및 라운드 전환, `unknown` 소비, 질문과 추측 혼합, 스무 번째 질문, 열아홉 질문 뒤 오답과 질문 재생이 기존 질문 비소비 동작을 드러냈다.
- 공통 활동 전이와 전체 기록 길이 불변식을 적용한 뒤 미스터리 판정기 34개가 모두 통과했다.
- 한국어 합성 낱말 2개와 영어 관사 부정 2개는 규칙 시험 44개 중 4개 실패로 재현했다. 경계와 관사 연결 보정 뒤 44개가 모두 통과했다.
- 이탈자가 점수에는 있지만 원래 차례에만 빠진 세 참가자 상태는 정상 변경으로 처리되어 1개가 실패했다. 원래 차례 포함 여부를 전달한 뒤 손상으로 거절했다.
- 자기 검토에서 이탈자가 원래 차례에 두 번 들어간 상태도 정상화되는 1개 실패를 추가로 확인했다. 정확히 한 번 있었을 때만 복원하도록 좁힌 뒤 미스터리 및 공통 판정기 79개가 모두 통과했다.

### 최종 확인

```text
npm test -- src/__tests__/mystery-box-rules.test.ts src/__tests__/question-game-room-engine-mystery.test.ts src/__tests__/question-game-room-engine.test.ts src/__tests__/question-game-room-response.test.ts src/__tests__/game-room-route.test.ts
PASS 5 files, 240 tests, FAIL 0

npm test
PASS 210 files, 1756 tests, FAIL 0

npx tsc --noEmit
통과

npx eslint src/lib/mystery-box-rules.ts src/lib/question-game-room-engine.ts src/lib/question-game-room-engines/mystery.ts src/__tests__/mystery-box-rules.test.ts src/__tests__/question-game-room-engine-mystery.test.ts
통과

git diff --check
통과
```

### 커밋

- `0806ea5` `fix: 미스터리 박스 활동 라운드 정합성 보강`

### 남은 우려

- 자유 질문 판정은 고정 표현 사전 기반이며 지원하지 않거나 뜻이 섞인 표현은 보수적으로 `unknown`을 반환한다.
- 화면과 데이터베이스 및 자료 이전 파일은 바꾸지 않았다.
